# InvestView Web

React TypeScript frontend for InvestView.

## Commands

```powershell
npm install
npm run dev
npm run build
npm run test
npm run lint
```

The development server proxies `/health` to the ASP.NET Core API at `http://localhost:5122`.
